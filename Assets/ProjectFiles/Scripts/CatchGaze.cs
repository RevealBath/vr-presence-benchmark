using UnityEngine;
using Tobii.G2OM;

public class CatchGaze : MonoBehaviour, IGazeFocusable
{
    public void GazeFocusChanged(bool hasFocus)
    {
        
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
